---

# Used to make figureing out distro a bit easier

linux_distro: {
  "CentOS": "rpm",
  "Amazon": "rpm",
  "Debian": "deb",
  "Ubuntu": "deb"
}

common_pkg: "*"       # Sets default to update all packages
allow_reboot: false   # Do not reboot by default
