---

# Used to make figureing out distro a bit easier

linux_distro: {
  "CentOS": "yum",
  "Amazon": "yum",
  "Debian": "apt",
  "Ubuntu": "apt"
}

mode: "install"
pkg_state: "latest"
allow_reboot: false   # Do not reboot by default
update: false
